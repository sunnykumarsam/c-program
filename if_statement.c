#include<stdio.h>
#include<conio.h>
int main(){
    int number=0;
    printf("Enter the number:-");
    scanf("%d",&number);
    if(number%2==0){
        printf("the number is even",number);
       if(number%2==)
                   printf("the number is odd",number);
        }
    }
    return 0;
}